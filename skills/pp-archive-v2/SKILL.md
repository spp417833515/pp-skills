---
name: pp-archive-v2
description: 任务收尾 v2(写总结 + 物理归档 2 合 1)。WIKI 同步已剥离到 /pp-sync-v2;本命令读 .pp/wiki/当前任务/[任务名]/ 3 份 MD + 证据/ · 写任务总结.md · task --archive · mv 到 .pp/wiki/任务归档/[任务名]_YYYY-MM-DD/。
disable-model-invocation: true
---

# /pp-archive-v2 — 任务收尾(写总结 + 物理归档)

## 定位

```
任务收尾(同步已由 /pp-sync-v2 完成)· 两件事(流程 §二 共 3 步):
  · 总结    LLM 推导本次任务收益 · 写 任务总结.md
  · 归档    .pp/wiki/当前任务/[任务名]/ → .pp/wiki/任务归档/[任务名]_<日期>/ + task --archive

⚠ WIKI 同步已剥离到 /pp-sync-v2 · 本命令不再碰 ppwiki wiki/decision。
  调用前确认已跑 /pp-sync-v2(资产入库);未跑 → 先去 /pp-sync-v2,再回来归档。
```

## 调用方式

```
/pp-archive-v2 <任务名>
```

验收通过 = 用户调用 /pp-archive-v2 即视为通过(信任 · 不重新验收)。

## 接口契约(移植先看这 · 三段式见 /pp-pipeline §五)

| 项 | 内容 |
|---|---|
| 输入 | 任务目录:3 份 MD + 证据/ 四份(齐全校验) |
| 输出 | 任务总结.md + 物理归档目录 `.pp/wiki/任务归档/[任务名]_日期/` |
| Store 接线 | ppwiki `task --archive` + `wiki --search` 校验 · 记账①② → 见 §Store 接线 |
| 缺依赖行为 | 无 ppwiki → 总结照写 · mv 照走(仍须用户拍板)· 记账跳过并声明 |

## 一、前置校验(任一失败即阻断)

| 检查 | 通过标志 |
|---|---|
| 任务目录存在 | `.pp/wiki/当前任务/[任务名]/` 在 |
| 3 份 MD 齐全 | 对话和澄清.md / 验收标准.md / 执行方案.md 都在 |
| 证据齐全 | 证据/{代码证据,外部证据,执行验收,审查报告}.md 在(缺 → 提示回对应阶段) |
| 已同步 | /pp-sync-v2 已跑(记账② 校验命中);未命中 → 提示先 /pp-sync-v2 |
| 归档目录无冲突 | `.pp/wiki/任务归档/[任务名]_<日期>/` 不存在 · 冲突 → 加 `-rescue-NNN` 后缀 |

## 二、3 步流程

```
Step 1 · 前置校验(上表)
Step 2 · 写 任务总结.md(落到 当前任务/[任务名]/ · 待归档时随 mv)
Step 3 · 记账①(task 置归档态)+ 询问用户 → mv 物理归档 → 验证 dst 存在且 src 已不在(不假设成功)
```

## 三、任务总结.md 模板(Step 2)

```markdown
# 任务总结 · <任务名>

日期:<YYYY-MM-DD>
状态:已归档
归档前路径:.pp/wiki/当前任务/<任务名>/
归档后路径:.pp/wiki/任务归档/<任务名>_<YYYY-MM-DD>/

## §1 · 本次任务做了什么(一句话)
<LLM 从 对话和澄清.md §1 + 执行方案.md §0 推导>

## §2 · 关键改动
| 类型 | 文件 | 说明 |
|---|---|---|
| 新建 | path:line | ... |
| 修改 | path:line | ... |
| 清理 | path:line | ... |

## §3 · 收益对比(改前 vs 改后)
| 维度 | 改前 | 改后 |
|---|---|---|
| ... | ❌ ... | ✅ ... |

## §4 · 已同步到 WIKI 的资产(引用 /pp-sync-v2 结果 · 本命令不再同步)
| WIKI/decision ID | 类型 | 说明 |
|---|---|---|
| 复用/函数库/.../xxx | 新建 | ... |
| decision/...        | 新建 | ... |

## §5 · 风险与遗留
- 无 / <列项>

## §6 · 下次任务可继承的经验
- <一行经验 · 防止重蹈覆辙>
```

## 四、物理归档命令(Step 3 · Bash;Windows 用 Git Bash 或 PowerShell Move-Item)

```bash
# 用户 A 后执行:
TODAY=$(date +%Y-%m-%d)
SRC=".pp/wiki/当前任务/[任务名]"
DST=".pp/wiki/任务归档/[任务名]_${TODAY}"

# 冲突处理:加 -rescue-NNN 后缀
if [ -d "$DST" ]; then
    N=1
    while [ -d "${DST}-rescue-${N}" ]; do N=$((N+1)); done
    DST="${DST}-rescue-${N}"
fi

mv "$SRC" "$DST"

# 验证(读事实不读记忆):落地才算成 · 否则报错阻断
if [ -d "$DST" ] && [ ! -d "$SRC" ]; then echo "✓ 归档落地: $DST"; else echo "✗ mv 未落地 · 阻断"; exit 1; fi
```

## Store 接线(ppwiki · 移植时改写/删除本节 · 方法论正文零 ppwiki)

| 记账点 | 时机 | op |
|---|---|---|
| ① | Step 3 置归档态 | `task --archive {task_id}`(默认校验 phase==P5 · 历史/手动归档 `force=true`) |
| ② | 前置校验 · 已同步 | `wiki --search {本任务关键词}`(命中 = 已 sync 的代理判定) |

- op / 参数以 `ppwiki('system --help', {module:'task'/'wiki'})` 为准 · 禁凭记忆
- 离线降级:①② 跳过并显式声明 · 总结与 mv 照走(仍须用户拍板)· 禁静默

## 五、用户视角对话样例

```
你: /pp-archive-v2 PPAgent纯MCP核心重构

AI: [写总结 + 物理归档 v2] 启动(同步已由 /pp-sync-v2 完成)

    进度 [●○○]  1/3 · 前置校验
    ✓ .pp/wiki/当前任务/PPAgent纯MCP核心重构/ 存在
    ✓ 3 份 MD + 证据/ 四份齐全
    ✓ 已同步(wiki --search 命中本任务资产)

    进度 [●●○]  2/3 · 写任务总结
    ✓ 任务总结.md 已写(关键改动 + 收益对比 + 继承经验)

    进度 [●●●]  3/3 · 归档
    ✓ ppwiki task --archive 已置归档态
    ┌──── 即将 mv ─────────────────────────────────────┐
    │ .pp/wiki/当前任务/PPAgent纯MCP核心重构/ →        │
    │ .pp/wiki/任务归档/PPAgent纯MCP核心重构_2026-06-04/│
    │ 含:3 份 MD + 证据/ 四份 + 任务总结.md(本次新写)  │
    └──────────────────────────────────────────────────┘
      A. ✅ 归档(推荐 · mv 物理搬过去)
      B. 🛑 先放着(总结已写 · 仅延后 mv)

你: A

AI: ✓ mv 完成 · 验证 dst 在 / src 已移除
    ─ 结论:任务闭环归档 · 验证:mv 落地✓ · 未处理:无 · 风险:无 · 下一步:开新任务
    🎉 任务收尾完成。
```

## 六、失败 / 边界

| 情况 | 处置 |
|---|---|
| 任务目录不存在 | 阻断 · 提示用户检查任务名 |
| 3 MD 任一缺失 | 阻断 · 提示先 /pp-plan-v2 补齐 |
| 证据缺份 | 阻断 · 提示回对应阶段补(execute/review) |
| 未同步(wiki 无本任务资产) | 阻断 · 提示先 /pp-sync-v2 |
| 归档目录已存在 | 自动加 `-rescue-NNN` 后缀 |
| 用户选 B | 任务总结已写 · 仅延后 mv · 后续可再喊 /pp-archive-v2 |

## 七、不做的事

- ❌ 不写业务代码
- ❌ 不重新验收(用户调用即视为通过)
- ❌ **不碰 wiki/decision 同步**(归 /pp-sync-v2)
- ❌ 不擅自 mv(必须用户拍 A)
- ❌ 不动 当前任务/ 和 任务归档/ 之外的文件

## 八、契约

```
上游:/pp-sync-v2 已入库(记账② 校验)+ 任务目录全产物
产出:任务总结.md + .pp/wiki/任务归档/[任务名]_日期/(任务闭环)
下游:无(链路终点)
全链路 / 目录结构 / 所有权 → /pp-pipeline
```
