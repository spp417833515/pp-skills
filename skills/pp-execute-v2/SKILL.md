---
name: pp-execute-v2
description: 执行 v2(循环跑命令式验收 → 全绿才过)。读 .pp/wiki/当前任务/[任务名]/ 执行方案.md + 验收标准.md · 按五类清单施工(复用>优化>修改>清理>新建)· 逐条跑 §5 命令式验收比对期望值 · 红就修根因不绕行 · ≥1 真实链路非 mock · 命令+实际输出落 证据/执行验收.md · 推进 task execute 阶段。
disable-model-invocation: true
---

# /pp-execute-v2 — 执行(循环跑命令式验收 → 全绿才过)

## 定位

```
不是"写完就算" · 是"逐条跑 验收标准.md §5 命令 · 比对期望值 · 红就回修 · 全绿才推进"
不是 mock 自欺 · 是真实运行(有外部依赖→≥1 真实 URL/KEY;纯内部任务→真实端到端跑)
不是黑箱 · 是每条验收命令的实际输出留存为证据
执行全程吃 CLAUDE.md 铁律:修根因 / 禁缝补 / 手术刀 / 底层先足量
```

## 调用方式

```
/pp-execute-v2 <任务名>
读 .pp/wiki/当前任务/[任务名]/{执行方案.md, 验收标准.md}
```

无任务目录 / 缺 MD → 阻断 · 提示先 /pp-plan-v2 产出三份 MD。

## 一、执行铁律(继承 CLAUDE.md · 命中即停回退)

| 铁律 | 触发即停 | 动作 |
|---|---|---|
| 修根因不绕行(红线1) | 报错想加 try-except 吞 + fallback | 查调用栈 → 定位根因 → 修逻辑 |
| 从根源(红线3) | 修了上游根因但下游还留 if"兼容"/绕行码 | 修上游 + **删下游所有绕行码**(非下游掩盖)|
| 禁缝缝补补(红线8) | 想加 wrap/hack/兼容旧版/双信源 | 停 → 回补底层 → 业务层一行 |
| 手术刀(红线6) | 顺手改邻接/格式/没坏的 | 只动 执行方案.md 列出的目标 |
| 分层不越级(红线7) | UI 直读 Store / UI 调逻辑层做业务 | 经业务层中转 |
| 执行顺序 | 跳着做 | 严按 执行方案.md §7:底层→业务→用户层 · 复用优先 |

## 二、5 步流程

```
Step 1 · 进阶段
         ppwiki('task --advance_phase', {…execute})   # op/参数以 system --help 为准

Step 2 · 按 执行方案.md 五类清单施工(复用 §1 > 优化 §2 > 修改 §3 > 清理 §4 > 新建 §5)
         每完成一层:ppwiki('task --fill_step' / 'task --finish_layer')
         清理清单 §4 = 直接删错误/冗余代码(不留 deprecated 二路)

Step 3 · 循环验收(核心 · 直到全绿)
         for 每条 验收标准.md §5 命令式验收:
             跑 Bash 命令 → 抓「实际输出」
             比对「精确期望值」
             ┌ 绿 → 记 ✓ · 下一条
             └ 红 → 定位根因 → 修 → 重跑(禁吞错 / 禁改期望值凑绿)· 同一条 3 次仍红 → 暂停列用户
         §6 真实运行:有外部依赖 → ≥1 真实 URL/KEY 跑通;纯内部任务 → 真实端到端跑 · 均非 mock

Step 4 · 提交前自检(公约自检清单 11 项 · "提交代码前逐项过")
         重点 7分层清晰 / 8无污染 / 9业务层一行 / 10底层足量 · 任一不过→回修(非记一笔放过)
         (自查 during 施工;review 阶段再做 hostile 外审 · 防御纵深 · 非双信源)

Step 5 · 全绿收口
         ppwiki('task --progress', {…})
         证据留存 → 证据/执行验收.md(命令 + 实际输出 + 真实链路原文)
         下一步:进 /pp-review-v2
```

## 三、证据留存(Step 4 · 落盘)

```
.pp/wiki/当前任务/[任务名]/证据/执行验收.md
```

| 验收项 | 命令 | 期望值 | 实际输出 | 结果 |
|---|---|---|---|---|
| schema 检测 | `grep -c schema_version x.json` | `1` | `1` | ✓ |
| 死代码清零 | `grep -rn old_fn .` | `(空)` | `(空)` | ✓ |
| 真实链路 | `curl -s $REAL_URL` | `200 + {ok:true}` | `200 {ok:true}` | ✓ |

> 真实链路输出原文(去敏后)附表末 · 作为"非 mock"的物证。

## 四、用户视角对话样例

```
你: /pp-execute-v2 PPAgent纯MCP核心重构

AI: [执行 v2] 启动 — 按执行方案五类清单施工 · 循环验收到全绿

    进度 [●○○○○]  1/5 · 进 execute 阶段 ✓
    进度 [●●○○○]  2/5 · 五类清单施工
       ✓ 复用 3 处 · 修改 2 处 · 清理 1 处(删旧 catalog)· 新建 1 处
    进度 [●●●○○]  3/5 · 循环验收(§5 共 7 条)
       [1/7] schema_version=1        期望 1      实际 1      ✓
       [2/7] 死代码 old_catalog       期望 空     实际 空     ✓
       [3/7] 真实链路 ppwiki health   期望 ok     实际 ok     ✓
       ...
       [5/7] 工具发现数               期望 ≥3     实际 0      ✗
         → 定位根因:注入点漏传 mcp_runtime(非加 fallback)
         → 修 · 重跑 → 实际 4 ✓
       7/7 全绿
    进度 [●●●●○]  4/5 · 提交前自检 11 项 ✓
    进度 [●●●●●]  5/5 · 证据留存
       ✓ 证据/执行验收.md(7 命令 + 实际输出)
    ┌──── 执行完成 · 全绿 ────┐
    │ 下一步:/pp-review-v2       │
    └─────────────────────────┘
```

## 五、失败 / 边界

| 情况 | 处置 |
|---|---|
| 验收命令始终红 | 不改期望值凑绿 · 定位根因 · 3 次仍红 → 暂停列给用户 |
| 外部依赖型任务无 KEY/URL | 阻断让用户提供;纯内部任务 → 真实端到端跑替代(仍非 mock) |
| 想加 fallback 绕过 | 红线8 命中 · 停 · 回补底层 |
| 执行方案缺某层能力 | 翻案:回 /pp-plan-v2 补底层项 · 不在业务层 hack |
| task op 报错 | 阻断 · `ppwiki('system --help',{module:'task'})` 核对 op/参数 |

## 六、不做的事

- ❌ 不改期望值凑绿(改的是代码不是验收)
- ❌ 不加 try-except 吞错 + fallback 兜底
- ❌ 不缝缝补补 / 不留 deprecated 二路
- ❌ 不跳执行顺序(底层→业务→用户层)
- ❌ 不用纯 mock 冒充真实链路
- ❌ 不顺手改 执行方案.md 之外的代码(手术刀)

## 七、与既有命令的关系

| 命令 | 阶段 | 定位 |
|---|---|---|
| /pp-clarify-v2 | 澄清/根因 | 三源调研 + 逻辑层单问 + 证据留存 |
| /pp-plan-v2 | 方案 | 3 份 MD + 子代理模拟验证 |
| **/pp-execute-v2** | 执行 | **本命令** · 循环跑命令式验收 → 全绿 |
| /pp-review-v2 | 审查 | 自我攻击审查 5 维 + 真实测试 + 跨任务回顾 |
| /pp-sync-v2 | 同步 | 提资产入 wiki + 证据留存 |
| /pp-archive-v2 | 归档 | 写总结 + 物理归档 |

```
唯一链路:/pp-clarify-v2 → /pp-plan-v2 → /pp-execute-v2 → /pp-review-v2 → /pp-sync-v2 → /pp-archive-v2
          澄清/根因      方案        执行          审查+回顾      同步        归档
```
